spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa

#  h2 콘솔 활성화
  h2:
    console:
      enabled: true

jwt:
  issuer: cksdnd@gmail.com
  secret_key: study-springboot